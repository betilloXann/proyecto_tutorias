import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // This is a basic smoke test that does nothing.
    // It's here to make sure the test runner has at least one test to run.
    expect(true, isTrue);
  });
}
