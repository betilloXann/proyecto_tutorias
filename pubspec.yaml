name: proyecto_tutorias
description: "gestion de tutorias de recuperacion"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.0.9

  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_analytics: ^12.0.4

  provider: ^6.1.2
  intl: ^0.20.2
  url_launcher: ^6.3.0
  smooth_page_indicator: ^1.1.0 # <-- RESTORING THIS

  cupertino_icons: ^1.0.8
  firebase_storage: ^13.0.4
  file_picker: ^10.3.7

  pdf: ^3.10.0
  printing: ^5.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.svg
